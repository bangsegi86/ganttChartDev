/** Shared pixel metrics that keep the grid rows and gantt bars aligned. */
export const ROW_HEIGHT = 32;
export const HEADER_HEIGHT = 52;
export const BAR_HEIGHT = 18;
export const BAR_VPAD = (ROW_HEIGHT - BAR_HEIGHT) / 2;
/** Width in px of the drag handles at each end of a bar. */
export const RESIZE_HANDLE = 10;
/** Extra space below the last row so it never sits flush against the bottom edge. */
export const SCROLL_BOTTOM_PADDING = 48;
